const router=require("express").Router(); const auth=require("../middleware/auth"); const Notification=require("../models/Notification"); const mongoose=require("mongoose");
router.get("/notifications",auth,async(req,res)=>{try{res.json(await Notification.find({recipient:req.user.id}).sort({createdAt:-1}).limit(50));}catch{res.status(500).json({message:"Failed to retrieve notifications"});}});
router.patch("/notifications/:id/read",auth,async(req,res)=>{if(!mongoose.Types.ObjectId.isValid(req.params.id))return res.status(400).json({message:"Invalid notification ID"});const note=await Notification.findOneAndUpdate({_id:req.params.id,recipient:req.user.id},{isRead:true},{new:true});if(!note)return res.status(404).json({message:"Notification not found"});res.json(note);}); module.exports=router;
